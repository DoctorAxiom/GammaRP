\documentclass[10pt, titlepage, openright]{book}
\usepackage[
left=1cm,
right=1cm,
top=1cm,
bottom=1cm,
paperwidth=17cm,
paperheight=24cm,
]{geometry}

\usepackage{lipsum}

\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{afterpage}
\usepackage{xfrac}
\usepackage{xcolor,sectsty}
\usepackage{gensymb}

\usepackage{textgreek}
\usepackage{svg}

\usepackage{tikz}
\usepackage{multicol}

\usepackage[T1]{fontenc}
\usepackage{kmath}
\usepackage{kerkis}

\usetikzlibrary{shapes.geometric}

\makeatletter
\def\closeopenmulticols{%
% test if current env is "multicols" if so close it
   \def\@tempa{multicols}%
   \ifx\@tempa\@currenvir
      \end{multicols}%
  \fi 
}
\makeatother

\rmfamily

%Command to insert a blank page
\newcommand{\blankpage}{%
	\null
	\thispagestyle{empty}%
	\addtocounter{page}{-1}%
	\newpage}

%Steven Segletes's Faux Small Caps command for situations where bolded small caps are needed.
\newcommand\fauxsc[1]{\fauxschelper#1 \relax\relax}
\def\fauxschelper#1 #2\relax{%
	\fauxschelphelp#1\relax\relax%
	\if\relax#2\relax\else\ \fauxschelper#2\relax\fi%
}
\def\Hscale{.85}\def\Vscale{.72}\def\Cscale{1.10}
\def\fauxschelphelp#1#2\relax{%
	\ifnum`#1>``\ifnum`#1<`\{\scalebox{\Hscale}[\Vscale]{\uppercase{#1}}\else%
	\scalebox{\Cscale}[1]{#1}\fi\else\scalebox{\Cscale}[1]{#1}\fi%
	\ifx\relax#2\relax\else\fauxschelphelp#2\relax\fi}

%QuickCommand for GammaRP with greek gamma
\newcommand{\GammaRP}{\textGamma RP }

%Dice commands
\newcommand{\dice}[2]{%
	\ifnum#2<0%
		D#1d\inteval{-1*(#2)}
	\else%
		D#1a#2
	\fi}

%Formatting a stat
\newcommand{\stat}[1]{ \{ \textsc{#1}  \} }


%% COLOR SET UP STUFF
\definecolor{grptitle}{RGB}{200,212,220}

\definecolor{chapterbgcolor1}{HTML}{C1FF85}
\definecolor{chapterbgcolor2}{HTML}{85FFFF}
\definecolor{chapterbgcolor3}{HTML}{C285FF}
\definecolor{chapterbgcolor4}{HTML}{FF8585}
\definecolor{chapterbgcolor5}{HTML}{FFED85}
\definecolor{chapterbgcolor6}{HTML}{85FFB0}
\definecolor{chapterbgcolor7}{HTML}{8597FF}
\definecolor{chapterbgcolor8}{HTML}{FF85D4}
\definecolor{chapterbgcolor9}{HTML}{85FFDC}
\definecolor{chapterbgcolor10}{HTML}{9F85FF}
\definecolor{chapterbgcolor11}{HTML}{FF85A8}
\definecolor{chapterbgcolor12}{HTML}{E5FF85}
\definecolor{chapterbgcolordefault}{HTML}{AAAAAA}

\newcommand{\ChapterBackgroundTint}{chapterbgcolordefault}

\def\SetChapterTint{%
	\ifnum\inteval{\arabic{chapter}-1}>12
		\renewcommand{\ChapterBackgroundTint}{chapterbgcolordefault}
	\else
		\ifnum\inteval{\arabic{chapter}-1}<1
			\renewcommand{\ChapterBackgroundTint}{chapterbgcolordefault}
		\else
			\edef\ChapterBackgroundTint{chapterbgcolor\inteval{\arabic{chapter}-1}}
		\fi
	\fi
}

\titleformat
	{\chapter} % command
	[block] % shape
	{\bfseries\LARGE} % format
	{} % label
	{1em} % sep
	{
		\rule{\textwidth}{1pt}
	    \centering 
	    \thechapter :
	} % before-code
	[
	\vspace{-0.5ex}%
	\rule{\textwidth}{0.3pt}
	] % after-code
\titlespacing{\chapter}{0pt}{1ex}{1ex}

%New chapter command for book chapters
\newcommand{\GRPchapter}[1]{%

	\closeopenmulticols
	\begin{multicols}{2}[\chapter{#1}]}

\AtEndDocument{\closeopenmulticols}

\title{%
	\includesvg[width=3.0in]{GammaRPTitleLogo} \\
	\color{grptitle}\textbf{Gamma RP} \\
	1st Edition \\
	\large A simple roleplaying system meant to be dynamic and expansible}

\author{Dr. A. Cory Axiom}

\begin{document}
	\AddToHookNext{shipout/background}{
	  \begin{tikzpicture}[remember picture,overlay]
	  \node at (current page.center) {
	    \includesvg[height=\pdfpageheight]{TitleBG}
	  };
	  \end{tikzpicture}
	}
	\color{grptitle}
	\maketitle
	\afterpage{\blankpage}

	\color{black}
	\pagenumbering{roman}
	\AddToHook{shipout/background}{
		\begin{tikzpicture}[remember picture,overlay]
		\node[shading = axis, rectangle, left color = \ChapterBackgroundTint, right color = \ChapterBackgroundTint!0, anchor = west, minimum height=\paperheight, minimum width = 1.5cm]  at (current page.west) {};
		\node[shading = axis, rectangle, right color = \ChapterBackgroundTint, left color = \ChapterBackgroundTint!0, anchor = east, minimum height=\paperheight, minimum width = 1.5cm]  at (current page.east) {};
		\node[shading = axis, trapezium, trapezium stretches=true, trapezium angle=-83.8, top color = \ChapterBackgroundTint, bottom color = \ChapterBackgroundTint!0, anchor = north, minimum height=1.5cm, minimum width = \paperwidth]  at (current page.north) {};
		\node[shading = axis, trapezium, trapezium stretches=true, trapezium angle=83.8, bottom color = \ChapterBackgroundTint, top color = \ChapterBackgroundTint!0, anchor = south, minimum height=1.5cm, minimum width = \paperwidth]  at (current page.south) {};
		\end{tikzpicture}
	}
	\AddToHook{cmd/chapter/before}{
		\SetChapterTint
	}
	\tableofcontents
	\afterpage{\blankpage}

	\newpage
	\pagenumbering{arabic}
	\include{Introduction}
	\include{TheBasics}

\end{document}